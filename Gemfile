source "https://rubygems.org"

gem 'pry-byebug' # for debugging
gem 'pg' # our postgresql gem that AR needs to connect to postgres
gem 'activerecord' # Dur?

# Gives me ActiveRecord Rake commands a la rails
gem 'standalone_migrations'

# Fake data generator, used in db/seeds.db
gem 'faker'
